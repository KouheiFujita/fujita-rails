<%= form_with(model: report, local: true) do |form| %>
  <% if report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(report.errors.count, "error") %> prohibited this report from being saved:</h2>

      <ul>
      <% report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :entry_date %>
    <%= form.date_select :entry_date, id: :report_entry_date %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :report_user_id %>
  </div>

  <div class="field">
    <%= form.label :report %>
    <%= form.text_area :report, id: :report_report %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
